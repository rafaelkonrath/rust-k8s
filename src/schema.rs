table! {
    giftcards (id) {
        id -> Int4,
        gift_company -> Varchar,
        gift_id -> Varchar,
        gift_value -> Int4,
        gift_code -> Varchar,
        active -> Bool,
    }
}
